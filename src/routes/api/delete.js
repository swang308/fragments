// src/routes/api/delete.js

/**
 * Handles deleting a fragment.
 */
module.exports = (req, res) => {
  // Your logic to handle the DELETE request here.
  res.status(200).json({ message: 'Fragment deleted successfully' });
}
