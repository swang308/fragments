// src/routes/api/put.js

/**
 * Handles updating a fragment.
 */
module.exports = (req, res) => {
  // Your logic to handle the PUT request here.
  res.status(200).json({ message: 'Fragment updated successfully' });
};
